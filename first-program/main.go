
// // প্রয়োজনীয় package থাকতে হবে
// package main  // এই ফাইলটি main package এর, অর্থাৎ এটি executable program তৈরি করবে

// // main go file এর package এর মধ্যে main function থাকা আবশ্যক
// // main() function থেকে প্রোগ্রাম শুরু হয়

// // fmt মানে হলো format
// // fmt package এ অনেক built-in function আছে যেমন: প্রিন্ট করা, ফরম্যাটিং ইত্যাদি
// import "fmt"

// // func মানে function
// // main() হলো প্রোগ্রামের entry point, যেখান থেকে কোড এক্সিকিউট হবে
// func main() {
//     // fmt.Println() function দিয়ে console/terminal এ লেখা প্রিন্ট করা হয়
//     // এটি লেখা শেষ হলে নতুন লাইনে চলে যায়
//     fmt.Println("Hello Bangladesh") // এখানে "Hello Bangladesh" প্রিন্ট হবে







//     // ========================= Variable and Data Type Start =========================


//       // ========================= Variable =========================
//     // ভ্যারিয়েবল হচ্ছে কম্পিউটারের RAM এ একটা জায়গা (box/ঘর)
//     // যেখানে আমরা ডেটা রাখি এবং একটা নাম দেই যাতে পরে সেই ঘর থেকে ডেটা নিতে পারি।
//     // উদাহরণ: a := 55 মানে — "RAM এ একটা ঘর বানাও, নাম দাও a, আর ভিতরে রাখো 55"

//     a := 55
//     fmt.Println(a) // 👉 Output: 55

//     // ভেতরে কী হচ্ছে:
//     // CPU → RAM এর মধ্যে একটা জায়গা তৈরি করছে
//     // RAM ঘর: [a | 55]
//     // মানে "a" নামের জায়গায় "55" রাখা আছে


//     // ========================= কম্পিউটারের ভিতরে কী থাকে =========================
//     // কম্পিউটার মূলত তিনটি বড় অংশে কাজ করে:
//     // 1️⃣ CPU (Processor): হিসাব করে, নির্দেশনা চালায়
//     // 2️⃣ RAM (Memory): কাজ চলার সময় অস্থায়ীভাবে ডেটা রাখে
//     // 3️⃣ Hard Disk / SSD: স্থায়ীভাবে ডেটা সংরক্ষণ করে
//     //
//     // ভ্যারিয়েবলগুলো থাকে RAM এর মধ্যে — প্রোগ্রাম চলার সময়।
//     // প্রোগ্রাম বন্ধ হলে RAM থেকে ডেটা মুছে যায়।
//     // কিন্তু ফাইল, ছবি, ডকুমেন্ট ইত্যাদি থাকে Hard Disk এ স্থায়ীভাবে।


//     // ========================= Data Type =========================
//     // ডেটা টাইপ হলো — ডেটা কেমন ধরনের হবে তা নির্ধারণ করে।
//     // যেমন সংখ্যা, দশমিক সংখ্যা, টেক্সট, হ্যাঁ/না ইত্যাদি।
//     //
//     // Go তে সাধারণত ৪ ধরনের ডেটা টাইপ বেশি ব্যবহার হয়:

//     // 1️⃣ int — পূর্ণ সংখ্যা (যেমন: 10, -5, 0)
//     // 2️⃣ float32 বা float64 — দশমিক সংখ্যা (যেমন: 3.14, -0.55)
//     // 3️⃣ bool — সত্য/মিথ্যা বা হ্যাঁ/না মানে (true / false)
//     // 4️⃣ string — লেখা বা টেক্সট (যেমন: "Hello World")


//     // 🔢 int টাইপ উদাহরণ:
//     var age int = 25
//     fmt.Println(age) // Output: 25

//     // 🧮 float32 টাইপ উদাহরণ:
//     var pi float32 = 3.1416
//     fmt.Println(pi) // Output: 3.1416

//     // ✅ bool টাইপ উদাহরণ:
//     x := true   // Go নিজে বুঝে নেয় এটা bool টাইপ
//     x = false   // মান পরিবর্তন করা গেল কারণ এটা const না
//     fmt.Println(x) // Output: false

//     // 🔤 string টাইপ উদাহরণ:
//     var name string = "Rasal Hossain"
//     fmt.Println(name) // Output: Rasal Hossain


//     // ========================= Constant =========================
//     // const মানে হলো "স্থায়ী মান" বা "যা পরিবর্তন করা যাবে না"
//     // এটা সাধারণত এমন মানের জন্য ব্যবহার হয় যা সবসময় একই থাকবে

//     const p int = 100
//     fmt.Println(p) // Output: 100

//     // নিচের লাইন আনকমেন্ট করলে Error আসবে, কারণ const পরিবর্তনযোগ্য না
//     // p = 300 // ❌ Error: cannot assign to p (constant)

//     // তাই const মানে — মান একবার নির্ধারণ করলে আর বদলানো যাবে না


//     // ========================= Short Variable Declaration =========================
//     // Go তে := দিয়ে দ্রুত ভ্যারিয়েবল ঘোষণা করা যায়
//     // যেমন a := 55 মানে হলো var a = 55
//     //
//     // এটা তখনই ব্যবহার করা যায় যখন Go নিজে টাইপটা আন্দাজ করতে পারে
//     // যেমন:
//     b := "Bangladesh"
//     fmt.Println(b) // Output: Bangladesh


//     // ========================= Memory Concept Summary =========================
//     // 🧠 যখন তুমি কোড রান করো:
//     // 1️⃣ তোমার কোড CPU তে যায়
//     // 2️⃣ CPU বলে “RAM, একটা ঘর তৈরি করো এবং 55 রাখো, নাম দাও a”
//     // 3️⃣ RAM মনে রাখে: a → 55
//     // 4️⃣ তুমি fmt.Println(a) লিখলে, CPU গিয়ে RAM থেকে a এর মান বের করে আনে → 55

//     // প্রোগ্রাম বন্ধ হয়ে গেলে RAM এর ডেটা (a, x ইত্যাদি) মুছে যায়।


//     // ========================= শেষ কথা =========================
//     // 👉 Variable = RAM এ ডেটা রাখার ঘর
//     // 👉 Data Type = ঘরে কী ধরনের জিনিস থাকবে তার ধরন
//     // 👉 Const = এমন ঘর যার ভিতরের জিনিস কখনও বদলানো যাবে না





//     // ========================= Variable and Data Type End =========================






// }



// ================================== if else switch start ====================================



package main 
import "fmt"


func main(){
  age := 18
  
  if age > 18 { // greather than to 18
    fmt.Println("You are aligible to be married")
  } else if age < 18 { // less than to 18
    fmt.Println("You are not eligible to married, but you can love some one")
  } else if age == 18 { // eqal to 18 
    fmt.Println("You are just a teenager, not eligible to be married")
  }

  // Operator
  // >, >=, <, <=, ==
  // and => && 
  // or => ||
  // not => !

  isPretty := false 

  if !isPretty {
    fmt.Println("ok")
  }else{
    fmt.Println("not")
  }


  x := 5
  xEqual :=5


  switch x {
  case 1: fmt.Println("x = 1")
  case 2: fmt.Println("x = 2")
  case 3: fmt.Println("x = 3")
  case 4: fmt.Println("x = 4")
  case xEqual: fmt.Println("x = 5")
  case 6: fmt.Println("x = 6")
  case 7: fmt.Println("x = 7")
  default: fmt.Println("Not")

  }



}




// ================================== if else switch end ====================================
